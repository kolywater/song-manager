# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate

# Build artifacts
build/
Export/

# Song data (large binary files)
Songs/

# Local test fixture — real Ableton project (~125MB of bounces/backups).
# Used by `just example` and the version unit tests when present locally.
example project/

# App icon source
appIcon.icon/

# Secrets — Dropbox token lives here, never commit
Song Manager iOS/DropboxConfig.swift
